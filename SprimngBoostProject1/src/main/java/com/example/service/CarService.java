package com.example.service;

import org.springframework.stereotype.Service;

import com.example.demo.pojo.Car;
@Service
public interface CarService 
{
	void createNewService(Car car);
	
	carRepo.findById(car.getCar)

}
